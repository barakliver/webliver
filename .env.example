# Supabase — the browser client only ever sees these two.
NEXT_PUBLIC_SUPABASE_URL=https://tjdnrktcwzoskvehvslr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# Server-only. Never expose to the browser: it bypasses RLS.
SUPABASE_SERVICE_ROLE_KEY=

# Receipt scanning (server-only).
ANTHROPIC_API_KEY=

# Public origin, used to build personalized RSVP links and the webcal feed.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
