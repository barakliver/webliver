# להוריד את האתר כאפליקציה, ולהעלות אותו לחנויות

## חלק א: להתקין עכשיו, בלי חנות ובלי עלות

האתר כבר בנוי כאפליקציה (PWA). אין מה להתקין ואין מה לאשר —
נכנסים לכתובת ומוסיפים למסך הבית. האייקון נפתח כאפליקציה מלאה,
בלי שורת כתובת, עם האייקון שלנו ועם שם משלה.

### אייפון ואייפד

1. לפתוח את `liverproductions.com` **בספארי**. חייב ספארי — בכרום באייפון
   אין את האפשרות הזאת.
2. ללחוץ על כפתור השיתוף למטה (ריבוע עם חץ כלפי מעלה).
3. לגלול ולבחור **הוספה למסך הבית**.
4. לאשר את השם ולחיצה על **הוספה**.

### אנדרואיד

1. לפתוח את `liverproductions.com` בכרום.
2. כרום יציע מעצמו **התקנת האפליקציה**. אם לא הופיע: תפריט שלוש הנקודות
   ואז **התקנת האפליקציה** או **הוספה למסך הבית**.

### מחשב (Windows או Mac)

1. לפתוח את הכתובת בכרום או ב-Edge.
2. בקצה שורת הכתובת מופיע אייקון התקנה קטן. ללחוץ ואז **התקנה**.

מה שנדרש לזה כבר עומד בתוקף ונבדק בדפדפן אמיתי: קובץ manifest תקין,
שלושה אייקוני PNG (192, 512, ו-512 maskable), שני צילומי מסך,
ו-service worker רשום שנותן גם דף חלופי כשאין רשת.

---

## חלק ב: Google Play

זה המסלול הריאלי מבין השניים. האפליקציה נארזת כ-TWA, שזו בעצם
האפליקציה הזאת עצמה בתוך מעטפת אנדרואיד.

1. חשבון Google Play Console. תשלום חד פעמי של 25 דולר.
2. להיכנס ל-https://www.pwabuilder.com, להזין `liverproductions.com`,
   לבחור **Android** ואז **Generate Package**.
3. מתקבלים שני דברים: קובץ `.aab` להעלאה, וקובץ `assetlinks.json`.
4. את `assetlinks.json` לשמור בשורש המאגר בשם הזה בדיוק, ולהריץ שוב
   `bash /root/webliver/deploy.sh`. סקריפט הפריסה כבר יודע להעתיק אותו
   ל-`/.well-known/assetlinks.json`. בלי הקובץ הזה האפליקציה תיפתח עם
   שורת כתובת של דפדפן במקום כמסך מלא.
5. ב-Play Console: יצירת אפליקציה, העלאת ה-`.aab`, מילוי תיאור, וצירוף
   האייקון וצילומי המסך שכבר יש במאגר.

שווה לדעת מראש: חשבון פרטי חדש ב-Google Play עובר תקופת בדיקה סגורה
עם קבוצת בודקים לפני שאפשר לפרסם לכולם. חשבון על שם עוסק או חברה
פטור ממנה. התנאים המדויקים מופיעים ב-Play Console עצמו בזמן ההרשמה.

---

## חלק ג: App Store של אפל

כאן צריך לומר את זה ישר: זה המסלול היקר והפחות בטוח.

מה שנדרש:
1. Apple Developer Program, 99 דולר לשנה, מתחדש.
2. מחשב Mac עם Xcode. אין דרך לעקוף את זה.
3. PWABuilder מייצר גם חבילת iOS, אבל את ההעלאה עצמה עושים מ-Xcode.

**הסיכון האמיתי:** לאפל יש כלל שנקרא Guideline 4.2, והוא אומר
שאפליקציה שהיא בעיקר אתר ארוז נדחית. אפליקציה שכל מה שהיא עושה
זה להציג את האתר תיתקל בו. כדי לעבור צריך שיהיה בה משהו שהוא באמת
אפליקציה: התראות דחיפה אמיתיות, עבודה במצב לא מקוון, שימוש במצלמה
להעלאת תמונות למודבורד, שמירת קבצים למכשיר. אלה דברים שאפשר לבנות,
אבל זו עבודה בפני עצמה ולא אריזה של הקיים.

**ההמלצה:** להתחיל מחלק א. הוא מיידי, חינם, ונותן לזוגות בדיוק את
מה שהם צריכים — אייקון על המסך שנפתח כאפליקציה. אם בהמשך חשוב
שהאפליקציה תהיה **נמצאת בחיפוש** בחנות, ללכת ל-Google Play.
את App Store לשמור לשלב שבו נבנו התראות דחיפה ועבודה לא מקוונת,
כי בלעדיהן הסיכוי לדחייה גבוה והכסף כבר שולם.

---

## מה כבר מוכן במאגר

| קובץ | תפקיד |
|---|---|
| `manifest.json` | שם, אייקונים, צילומי מסך, קיצורי דרך, צבעים |
| `sw.js` | service worker: תנאי ההתקנה ודף חלופי בלי רשת |
| `icon-192.png`, `icon-512.png`, `icon-maskable-512.png` | אייקוני האפליקציה |
| `screenshot-narrow.png`, `screenshot-wide.png` | לחלון ההתקנה של כרום ולדף החנות |
| `deploy.sh` | פריסה לשרת, כולל `.well-known/assetlinks.json` כשיהיה |
