# איפה אנחנו

עודכן: 23 באוגוסט 2026, אחרי סבב הנתונים והחמ״ל.

הקובץ הזה נכתב כדי שאפשר יהיה להמשיך מחר בלי לשחזר שום דבר מהזיכרון.

---

## המצב בשורה אחת

הקוד מוכן ודחוף. **הסכמה כבר רצה בפרודקשן.** הקוד עצמו — לא בטוח שנפרס,
וצריך לוודא את זה קודם כל.

---

## גיט

- **ענף:** `claude/general-fixes-kqxoqc`
- **קומיט אחרון:** `542f0f5` — מנוי ליומן
- השרת נבדק לאחרונה על `85d8ce4`. מאז נוספו **קומיטים חדשים ושלוש מיגרציות**
  (0027, 0028, 0029), אז **חייבים להריץ `setup.sql` מחדש לפני שפורסים קוד**:
  הקוד החדש מצפה ל-`tasks.visible_to_client`, ל-`calendar_feeds`
  ול-`day_schedule.done_at`.

## השרת

- DigitalOcean, `164.92.132.64`, **1GB RAM**
- תיקיית העבודה: `~/webliver/liver-next`
- השירות: `liver-next`
- **הענף המקומי בשרת נקרא `refactor-v2`** ולא כשם הענף המרוחק. זה בלבל אותנו
  פעם אחת, שווה לזכור.

## Supabase

- פרויקט **liver**, `tjdnrktcwzoskvehvslr`
- **`setup.sql` הורץ בהצלחה** — אבל בגרסה של 26 מיגרציות.
- **הגרסה הנוכחית: 29 מיגרציות, 3,473 שורות.** צריך להריץ אותה שוב.
  היא בנויה להרצה חוזרת ולא הורסת נתונים קיימים.

---

## הדבר הראשון לעשות מחר

**לוודא איזה קוד רץ בשרת:**

```bash
cd "$(systemctl show liver-next -p WorkingDirectory --value)" && git log --oneline -1
```

אם זה לא `542f0f5` או חדש יותר, הפריסה היא:

```bash
cd ~/webliver
git status --short          # צריך להיות ריק
git fetch origin
git checkout -B claude/general-fixes-kqxoqc origin/claude/general-fixes-kqxoqc
cd liver-next
npm ci                      # חובה — נוספה תלות חדשה
npm run build               # אם נהרג בזיכרון, צריך swap
systemctl restart liver-next
npm run verify              # 25 בדיקות
```

`.env.local` לא בגיט ולא ייגע.

---

## שאלה פתוחה עם משתמש אמיתי

**"ניסיתי להיכנס עם מייל אחר, הוא לא נותן."** לא נסגר.

שתי סיבות אפשריות, ופקודה אחת מפרידה ביניהן:

```bash
journalctl -u liver-next -n 80 --no-pager | grep -i auth
```

- **יש `[auth] signInWithOtp failed`** → שרת המייל של Supabase חוסם. הוא מוגבל
  לכשתי הודעות בשעה ובפועל מסרב לכתובות שאינן חברות בארגון. התיקון הוא SMTP
  משלך, לא קוד.
- **אין שורה כזו** → הכניסה הצליחה והמשתמש נחת על "החשבון ממתין לאישור". זו
  ההתנהגות שנבנתה בכוונה. זוג נכנס רק אחרי שצירפו את המייל שלו בתוך אירוע;
  מפיק נכנס רק אחרי אישור בקונסול.

---

## חסום עלינו, לא על הקוד

1. **שלוש הגדרות ב-Supabase** (ראו `DEPLOY.md`): אורך קוד 6, תוקף 900 שניות,
   ו-SMTP של Resend. השלישית פותרת את "שליחת המייל אינה מוגדרת בשרת".
2. **מפתח Anthropic** — בלעדיו העוזר הדיגיטלי מפנה לוואטסאפ. לא שבור.
3. **חומר וידאו לרקע** — התשתית בנויה, מופעלת עם `NEXT_PUBLIC_HERO_VIDEO`.
4. **החלפת מפתח ה-Resend** שנחשף בצילום מסך. **עדיין פתוח.**
5. **דומיין** — לפי בקשה מפורשת, אחרון שבאחרונים.

---

## נבנה עכשיו (מחכה לפריסה)

1. **מסך נתונים** — `/app/insights`. משפך מפנייה לחוזה, מאיפה מגיעה העבודה,
   זמן תגובה חציוני, כסף שנגבה / פתוח / באיחור, ומה דורש טיפול. **שום אחוז
   לא מוצג מתחת לחמישה מקרים** — במקומו מוצג המספר עצמו.
2. **חמ״ל האירוע** — `/app/clients/<id>/live`. מה קורה עכשיו, הבא בתור, סימון
   שורה כבוצעה (עם השעה שבה באמת קרתה), ורשימת צוות וספקים אחת לפי שעת הגעה
   עם חיוג ווואטסאפ. **למפיק בלבד** — שיבוץ צוות לא עובר לזוג.
3. **מנוי ליומן** — `/feed/<token>.ics`. אפליקציית יומן מושכת בלי עוגייה, אז
   הכתובת עצמה היא המפתח: ארוכה, אקראית, וניתנת לביטול בלחיצה.
4. **סרגל תחתון בטלפון** — ארבע לשוניות ו״עוד״. שמונה יעדים על מסך של 390
   פיקסל נתנו לכל אחד 48 פיקסל ותווית קטועה.

## הבא בתור לבנייה

1. חנות והזמנות — הטבלה קיימת, המסך לא
2. מתנות דיגיטליות ב-RSVP (PayBox / ביט + ברכה)
3. מיתוג לבן למפיקים (לוגו וצבע משלהם)
4. שליטה על פיצ׳רים מהקונסול של מנהל המערכת
5. סורק קבלות (צילום → חילוץ סכום וספק → שורה בתקציב) — דורש מפתח Anthropic
6. שלוש הטבלאות שנשארו מקובץ הוורד: רגעי מוזיקה, צ׳ק ליסט ציוד, פרטים אישיים

**קיים כבר ואין צורך לבנות מחדש:** לוח השראה, אורחים, ייבוא CSV, קישורי RSVP
אישיים, העדפות תזונה, הושבה, לוז לפי תפקיד עם הדפסה, ספירה לאחור, צוות
וספקים, עורך תוכן לאתר, עוזר דיגיטלי, מחשבון בר ואלכוהול, תיק אירוע כטמפלט.

---

## איך בודקים שכלום לא נשבר

```bash
npm run verify      # בשרת: כל מסך, הפלטה שבאמת רצה, והמנוי ליומן
npm test            # 87 בדיקות על החוקים הטהורים
npm run contrast    # ניגודיות הצבעים
npm run typecheck
```

וב-Supabase: להדביק את `supabase/verify.sql`. קורא בלבד, לא משנה כלום.
מדפיס 24 שורות, כולל אחת שמוודאת שזוג לא יכול להגיע לצוות ולעלויות.


---

## שאלה אחת שמחכה לך

בשלב 4 ביקשת ש**מנהל המערכת לא יוכל לראות נתונים של זוגות אצל מפיקים אחרים.**
זה סותר הוראה קודמת שלך שלפיה `barakliver@gmail.com` מקבל הרשאה לכל דבר
במערכת, והיא בנויה כך היום: **55 מדיניות על 17 טבלאות** נשענות על
`is_super_admin()`.

הצעת ביניים, ואני מיישם ברגע שתאשר:

- **רוט מפסיק לראות:** אורחים, תקציבים, חוזים, הודעות, צוות וספקים של מפיקים
  אחרים.
- **רוט ממשיך לראות:** רשימת המפיקים, סטטוס האישור שלהם, ומספרים מצטברים
  (כמה אירועים, כמה לידים) בלי שמות.

הסיבה שלא עשיתי את זה לבד: **אם רוט לא רואה כלום, רוט לא יכול לתמוך באף אחד.**
